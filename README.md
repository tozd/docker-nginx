You can extend this image and put files under `/etc/nginx/sites-enabled/` to add custom sites.

Alternativelly, you can mount a volume into `/etc/nginx/sites-volume/` directory and provide sites there.
